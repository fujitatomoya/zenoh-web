{
  "name": "zenoh-web",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "hugo --cleanDestinationDir && npx pagefind --site public",
    "check:dist": "node scripts/check-dist.mjs",
    "dev": "hugo server"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.16",
    "pagefind": "^1.3.0",
    "tailwindcss": "^3.4.17"
  },
  "overrides": {
    "picomatch": "^2.3.2",
    "tinyglobby": {
      "picomatch": "^4.0.4"
    }
  }
}
